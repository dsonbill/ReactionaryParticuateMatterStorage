using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExteriorFiniteDefintion : MonoBehaviour
{
    //Unlinked external shape / what the system assumes it's external structure to be viewed as
    //May or may not already be handled

    //Shape based on absolution linker type

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
