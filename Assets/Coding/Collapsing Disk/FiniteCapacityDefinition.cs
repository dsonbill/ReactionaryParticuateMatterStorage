using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FiniteCapacityDefinition : MonoBehaviour
{
    //Defines a space to collect the variable wave function that we are targeting

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
