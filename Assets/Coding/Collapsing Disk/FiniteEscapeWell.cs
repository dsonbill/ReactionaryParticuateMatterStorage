using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FiniteEscapeWell : MonoBehaviour
{
    //Targeted existence absolution mechanisms
    //Also described as links to the external world
    
    //Diameter
    //Contact depth
    //Escape potential
    //Relative position


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
