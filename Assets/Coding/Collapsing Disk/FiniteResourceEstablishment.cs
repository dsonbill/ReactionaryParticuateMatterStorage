using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FiniteResourceEstablishment : MonoBehaviour
{
    //Reconfigures spacial definition to accomodate selected variable wave function
    //This is where the contacting happens

    //Matches the shape of the variable wave function

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
