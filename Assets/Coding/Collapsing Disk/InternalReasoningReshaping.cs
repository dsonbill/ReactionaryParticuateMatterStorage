using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InternalReasoningReshaping : MonoBehaviour
{
    //Maps logical pathways between one internal reasoning and another
    //Based on changing beliefs of pathway subjects

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
