using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PhotonicNumericLink : MonoBehaviour
{
    //Classifies, finds, or acknowledges links between variable wave function and inherent features in mathematics, data, and numbers

    //All of it? --> 623

    //tt

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
