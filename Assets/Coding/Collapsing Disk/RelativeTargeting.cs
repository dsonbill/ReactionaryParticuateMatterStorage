using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RelativeTargeting : MonoBehaviour
{
    //Finds relative location to desired information, from current established center


    //Built upon knowledge that we already have

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
