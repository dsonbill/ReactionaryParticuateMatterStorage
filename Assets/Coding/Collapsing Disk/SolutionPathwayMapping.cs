using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SolutionPathwayMapping : MonoBehaviour
{
    //Automatically defines solution pathway based on previous systems and absolved knowledge

    //Moves backwards from the target information

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
