using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TargetingWell : MonoBehaviour
{
    //Analyses and selects the variable wave function that had achieved our goals, based on absolved knowledge

    //Selects VWF based on systtematic appearances of points on VWF relative to internal time, not entire wave function behaviour

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
