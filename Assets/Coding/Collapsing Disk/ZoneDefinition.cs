using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZoneDefinition : MonoBehaviour
{
    //External and internal physical structure shaping
    // Start is called before the first frame update

    //Central area
    //Variable-distance midpoint
    //Exterior contact
    //Dimensionally-separate "midpoint" (VWF Garden area)
    //Border width/depth

    //Densities of spacetime in all sections

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
