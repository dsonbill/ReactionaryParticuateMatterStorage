using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinkerEngine : MonoBehaviour
{
    //Connects Logical Existence Descriptors with each other and Begins the singular containment unit
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
